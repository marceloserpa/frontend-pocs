# BrowserStack App Automate credentials (Account > Settings on browserstack.com)
BROWSERSTACK_USERNAME=
BROWSERSTACK_ACCESS_KEY=

# Path to the signed .ipa to upload for a run (relative to repo root, or bs://<app_id> to reuse a previous upload)
BROWSERSTACK_APP_PATH=./build/todo-poc.ipa
